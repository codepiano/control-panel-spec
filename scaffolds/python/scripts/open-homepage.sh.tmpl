#!/usr/bin/env bash
set -euo pipefail

target="{{frontendUrl}}"
if [ -z "$target" ]; then
  target="{{homepageUrl}}"
fi

if [ -z "$target" ]; then
  echo "No homepage URL configured" >&2
  exit 1
fi

open "$target"
