# Python Scaffold

This scaffold is for Python services or local tools that can expose a tiny HTTP metrics endpoint.

Expected pieces:

- `control-panel.json.tmpl`
- `scripts/*.sh.tmpl`
- `metrics-server.py.tmpl`

Prefer `python3` and keep the endpoint small and explicit.
